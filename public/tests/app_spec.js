describe('LearnJS', function() {  it('invokes the router when loaded', function() {    spyOn(learnjs, 'showView');    learnjs.appOnReady();    expect(learnjs.showView).toHaveBeenCalledWith(window.location.hash);  });  it('can show a problem view', function() {    learnjs.showView('#problem-1');    expect($('.view-container .problem-view').length).toEqual(1);  });  it('shows the landing page view when there is no hash', function() {    learnjs.showView('');    expect($('.view-container .landing-view').length).toEqual(1);  });  it('passes the hash view parameter to the view function', function() {    spyOn(learnjs, 'problemView');    learnjs.showView('#problem-42');    expect(learnjs.problemView).toHaveBeenCalledWith('42');  });  it('subscribes to the hash change event', function() {    learnjs.appOnReady();    spyOn(learnjs, 'showView');    $(window).trigger('hashchange');    expect(learnjs.showView).toHaveBeenCalledWith(window.location.hash);  });  describe('problem view', function() {    var view;    beforeEach(function () {      view = learnjs.problemView('1');    });    it('has a title that includes the problem number', function() {      expect($.trim(view.text()).startsWith('Problem #1')).toBeTruthy();    });    describe('answer section', function() {      it('can check a correct answer by hitting a button', function() {        view.find('.answer').val('true');        view.find('.check-btn').click();        expect($.trim(view.find('.result').text()).startsWith('Correct!')).toBeTruthy();      });      it('rejects an incorrect answer', function() {        view.find('.answer').val('false');        view.find('.check-btn').click();        expect(view.find('.result').text()).toEqual('Incorrect!');      });    });// answer section    describe('correct flash section', function() {      it('guides the user to the next problem', function() {        expect(learnjs.buildCorrectFlash(1).find('a').attr('href')).toEqual('#problem-2');      });      it('guides the user to the landing page after the last problem', function() {        expect(learnjs.buildCorrectFlash(learnjs.problems.length).find('a').attr('href')).toEqual('');      });    });// correct flash section  });// problem view});// LearnJS